$ git branch
* main
$ git switch -c feature-XYZ
$ git branch
* feature-XYZ
  main
$ git push -u origin feature-XYZ
$   vi feature-XYZ.txt → git add . → git commit -m "..."
$   vi feature-XYZ.txt → git add . → git commit -m "..."
$ git push
 und AsciiDoc
